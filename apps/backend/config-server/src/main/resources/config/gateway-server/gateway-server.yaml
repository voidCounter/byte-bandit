example:
  property: I AM THE DEFAULT
spring:
  application:
    name: gateway-server
server:
  port: 8084
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
management:
  endpoint:
    web:
      exposure:
        include: health,info
