spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
eureka:
  client:
    enabled: false