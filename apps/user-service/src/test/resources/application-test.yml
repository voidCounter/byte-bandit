spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test_oakcandb
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 12345
  cloud:
    discovery:
      enabled: false
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  config:
    import: classpath:secrets.yml
